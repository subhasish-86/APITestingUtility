<UserControl x:Class="TestHarness.WPF.UC.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
            <ScrollViewer>
                <TextBox Text="{Binding Result, Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" />
                <!--<DataGrid ItemsSource="{Binding Result,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" AutoGenerateColumns="True">
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                        <DataGrid ItemsSource="{Binding Properties,Mode=OneWay,UpdateSourceTrigger=PropertyChanged}" AutoGenerateColumns="True">
                            
                        </DataGrid>
                    </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>-->
            </ScrollViewer>
    </Grid>
</UserControl>
